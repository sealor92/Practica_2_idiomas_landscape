<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="textViewStyle" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/green</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="textViewStyler" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/red</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="textViewStyleb" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/red</item>
        <item name="android:typeface">monospace</item>
        <item name="android:background">@color/black</item>
    </style>

</resources>
